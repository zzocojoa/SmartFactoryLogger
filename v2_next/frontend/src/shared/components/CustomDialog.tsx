import React from 'react';
// import { CustomDialogView } from '...';
// import { useCustomDialogModel } from '...';

export const CustomDialog: React.FC = () => {
  // const model = useCustomDialogModel();

  // if (!model.isOpen) {
  //   return null;
  // }

  return null; // <CustomDialogView model={model} />;
};
