export type SpotState = {
  label: string;
  statusClass: string;
  fillClass: string;
  warning: boolean;
  sparkClass: string;
};

export type LevelState = {
  label: string;
  className: string;
};
